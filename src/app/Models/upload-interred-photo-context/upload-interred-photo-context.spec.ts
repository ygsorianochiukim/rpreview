import { UploadInterredPhotoContext } from './upload-interred-photo-context';

describe('UploadInterredPhotoContext', () => {
  it('should create an instance', () => {
    expect(new UploadInterredPhotoContext()).toBeTruthy();
  });
});
